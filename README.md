# Atomgrid Transporter Finder

Web-based PTL transporter lookup tool for Atomgrid domestic logistics operations.

## Run locally

```bash
npm install
npm run dev
```

## Build

```bash
npm run build
```

The production output is generated in `dist/` and can be deployed as a Render Static Site.
